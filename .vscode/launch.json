{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch test function",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}",
            "args": [
                "-test.run",
                "MyTestFunction"
            ],
            "console": "integratedTerminal" // เพิ่มบรรทัดนี้เพื่อให้ผลลัพธ์แสดงใน terminal
        }
    ]
}
